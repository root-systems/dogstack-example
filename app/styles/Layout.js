export default {
  container: (props) => ({
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%'
  }),
  wrapper: ({ theme }) => ({
    display: 'flex',
    flexDirection: 'column',
    paddingTop: 64
  })
}
